<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <application
        android:label="AM Logistics Driver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Default launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
                Android App Links (HTTPS) — Verified deep links for:
                https://amlogistics.co.in/driver/session/<TOKEN>

                android:autoVerify="true" enables automatic verification so Android
                opens the app directly without a chooser dialog (Android 6+).

                REQUIREMENT: https://amlogistics.co.in/.well-known/assetlinks.json
                must be deployed with:
                  - package_name: "in.amlogistics.driver"
                  - sha256_cert_fingerprints: [Google Play App Signing certificate SHA-256]

                Until assetlinks.json is live with the correct fingerprint, Android 12+
                will open the link in the browser instead of the app.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="amlogistics.co.in"
                    android:pathPrefix="/driver/session/"/>
            </intent-filter>

            <!--
                Fallback: http scheme (handles test links and older Android versions).
                Also handles the case where the domain redirects http→https before
                the app intercepts the intent.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="http"
                    android:host="amlogistics.co.in"
                    android:pathPrefix="/driver/session/"/>
            </intent-filter>

            <!--
                Custom URL scheme fallback: amlogistics://driver/session/<TOKEN>
                Works without assetlinks.json verification.
                Use this for testing before assetlinks.json is deployed.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="amlogistics"
                    android:host="driver"
                    android:pathPrefix="/session/"/>
            </intent-filter>
        </activity>

        <!-- Foreground location service -->
        <service
            android:name=".LocationForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required to check if Google Play Store is installed for fallback -->
        <package android:name="com.android.vending"/>
    </queries>
</manifest>
